.anchor
.DS_Store
target
node_modules
dist
build
test-ledger
public/vendor/

# Repo-root legacy files use a different (4-space) style and are intentionally
# not covered by CI lint (which only checks */*.js). Keep the hook from
# reformatting them.
/*.js
